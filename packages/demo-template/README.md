# Demo Template Package

The backend currently generates demo files directly from `apps/backend/src/services/generator.ts`.

As the project grows, move reusable generated-app templates here:

- React dashboard shell
- Node API proxy shell
- README template
- claim report template
- demo script template
